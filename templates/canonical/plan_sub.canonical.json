{
  "mova_version": "3.3",
  "parameters": {
    "type": "object",
    "properties": {
      "name": {
        "type": "string"
      }
    },
    "required": [
      "name"
    ]
  },
  "actions": [
    {
      "type": "return",
      "invoke": "flow:return",
      "payload": "Привіт від допоміжного плану, {name}!"
    }
  ]
}